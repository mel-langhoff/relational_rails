<h1><%= @artist.name %>'s Show Page</h1>
<p><%= link_to "All Albums", artist_albums_path(@artist) %>
<p><%= link_to "Update Artist", edit_artist_path(@artist) %>
<p><%= link_to "Delete Artist", destroy_artist_path(@artist), method: :delete %>

<ol>
  <li>Still recording? <%= @artist.still_recording %></li>
  <li>Number of singles: <%= @artist.number_of_singles %></li>
  <li>Number of albums: <%= @artist.album_count %>
</ol>